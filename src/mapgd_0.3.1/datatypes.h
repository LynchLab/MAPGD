/* This file should include all data types that are supported for reading and writing 
 * to .map files. A supported data type should have an overloaded sizet(), and sizeb()
 * function that specify the size of the data type.   
 * readt(), and readb(), have the sizeof
 */

#ifndef DATA_TYPES_H_
#define DATA_TYPES_H_

#include "quartet.h"
#include "locus.h"
#include "population.h"	//FORMALLY allele_stat.
//#include 		//
#include "genotype.h"

#endif 
